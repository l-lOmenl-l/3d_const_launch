<UserControl x:Class="_3dconst_launch.Circular"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:_3dconst_launch" xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="150" d:DesignWidth="150" Background="#111111">

    <UserControl.Resources>
        <Storyboard x:Key="LoadingAnimation">
            <DoubleAnimation
            Storyboard.TargetName="loadingPath"
            Storyboard.TargetProperty="(UIElement.RenderTransform).(RotateTransform.Angle)"
            From="0"
            To="360"
            Duration="0:0:1"
            RepeatBehavior="Forever"/>
        </Storyboard>
    </UserControl.Resources>

    
        <StackPanel x:Name="sp_main" 
                HorizontalAlignment="Center" 
                Visibility="Visible"            
                VerticalAlignment="Center">

        <materialDesign:Card Padding="10"
                             Width="120"
                             Background="#332D2D2D" 
                             UniformCornerRadius="100">
            <Grid>
                <TextBlock x:Name="tb_percent"  
                   Margin="0,5,0,0" 
                   Text="Message" 
                   FontFamily="Manrope"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                   TextAlignment="Center"
                   Foreground="#D1D1D1"
                   Visibility="Hidden"/>

                <ProgressBar x:Name="circular"
                         IsIndeterminate="True"
                         Style="{StaticResource MaterialDesignCircularProgressBar}"
                         Foreground="#17BC43"
                         Value="10"
                         Height="100"
                         Width="100"/>
            </Grid>

        </materialDesign:Card>
            

            
        <TextBlock x:Name="tb_message"  
                   Margin="0,20,0,0" 
                   Text="Message" 
                   FontFamily="Manrope"
                   TextAlignment="Center"
                   Foreground="#D1D1D1"
                   Visibility="Hidden"/>

    </StackPanel>

</UserControl>
